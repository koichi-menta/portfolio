---
description: mainブランチにチェックアウトして最新を取得する
---

mainブランチに切り替えて最新のリモート変更を取得して。

手順:
1. `git status` で未コミット・未追跡の変更がないか確認する
   - 変更がある場合は破棄せず、内容をユーザーに伝えて続行してよいか確認する
2. `git checkout main`
3. `git pull`
4. 結果（fast-forwardした内容やコンフリクトの有無など）を簡潔に報告する
